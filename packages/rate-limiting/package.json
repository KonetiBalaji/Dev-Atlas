{
  "name": "@devatlas/rate-limiting",
  "version": "1.0.0",
  "description": "DevAtlas Rate Limiting Package",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "redis": "^4.6.0",
    "ioredis": "^5.3.0",
    "express-rate-limit": "^7.1.0",
    "express-slow-down": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  }
}

